---
apiVersion: "networking.k8s.io/v1"
kind: "Ingress"
metadata:
  name: "server-ingress"
  namespace: "default"
  annotations:
    networking.gke.io/managed-certificates: "lindria-ssl"
spec:
  defaultBackend:
    service:
      name: "server-service"
      port:
        number: 5000